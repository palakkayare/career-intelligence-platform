from django.apps import AppConfig


class InterviewPrepConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.interview_prep"
    verbose_name = "Interview Preparation"
